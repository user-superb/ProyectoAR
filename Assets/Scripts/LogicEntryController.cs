using UnityEngine;

public class LogicEntryController : MonoBehaviour
{
    
    private Transform in1;
    private Transform in2;

void Start()
{
    //in1 = transform.GetChild(1);
    //in2 = transform.GetChild(2);
}


    // Update is called once per frame
    void Update()
    {   
        //bool in1_connected = in1.GetChild(0).GetComponent<ComportamientoEntradas>().isConnected;
        //bool in2_connected = in2.GetChild(0).GetComponent<ComportamientoEntradas>().isConnected;

    }
}
